---
title: "List projects"
openapi: "api-reference/openapi.json GET /api/v1/projects"
---

Returns your organization's projects (workspaces). Every endpoint that accepts a
`project_id` uses these ids — omit the parameter anywhere to operate in the
default project. See [Projects](/platform/projects) for what a project scopes.
